# Data Source Toggle
NEXT_PUBLIC_USE_MOCK_DATA=true

# Microsoft Fabric (production only — NEVER prefix secrets with NEXT_PUBLIC_)
FABRIC_API_URL=
FABRIC_TOKEN=
NEXT_PUBLIC_FABRIC_WORKSPACE=

# Service Principal Auth (fallback when Azure CLI is not available)
# Required only if `az account get-access-token` is not an option
FABRIC_TENANT_ID=
FABRIC_CLIENT_ID=
FABRIC_CLIENT_SECRET=
